export {};

// export function createQuerySearch = <Type>(search:Type, query:Type):void => {
//   let searchData = Object.entries(search);
//   for (let i = 0; i < searchData.length; i++) {
//     if (search[`${searchData[i][0]}`]) {
//       query.search[`${searchData[i][0]}`] = search[`${searchData[i][0]}`][0];
//     }
//   }
// };
